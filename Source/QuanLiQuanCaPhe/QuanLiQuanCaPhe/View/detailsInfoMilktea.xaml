<UserControl x:Class="QuanLiQuanCaPhe.View.detailsInfoMilktea"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLiQuanCaPhe.View"
             mc:Ignorable="d" 
			 d:DesignHeight="450" d:DesignWidth="800"
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
			TextElement.Foreground="{DynamicResource MaterialDesignBody}"
			TextElement.FontWeight="Regular"
			TextElement.FontSize="13"
			TextOptions.TextFormattingMode="Ideal"
			TextOptions.TextRenderingMode="Auto"
			Background="{DynamicResource MaterialDesignPaper}"
			FontFamily="{DynamicResource MaterialDesignFont}">
	<Grid Background="AntiqueWhite">
		<TextBlock Text="Du lieu nhan duoc tu cha"></TextBlock>
		<TextBlock FontSize="24" Text="{Binding ten,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBlock>
		<TextBlock FontSize="24" Text="{Binding tenTraSua,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBlock>
		<Button Width="80" Height="25" Command="{Binding btnClick_CLick}">Click</Button>
	</Grid>
</UserControl>
